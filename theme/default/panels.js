//makePanel( Unique ID for the panel (you can make this up, no spaces though), Applets separated by "|". For each applet, CSS can be added after a "?")

makePanel("bottomPanel","Menu|TaskList?width:360px;|Sound|Clock|MinMax");

// makePanel("topPanel","Menu:24px|Spacer:446px|MinMax:10px");  // example second panel